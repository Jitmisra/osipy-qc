# environments
.venv/
venv/
# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
# os
.DS_Store

# ---- your own scans / pipeline outputs ----
# Keep the folders in the repo, but NEVER commit anything dropped into them.
data/*
!data/.gitkeep
!data/README.md
output/*
!output/.gitkeep

# never commit real / patient data, wherever it lands
Data_for_Agnik/
*_S_*.nii.gz

# the frontend build is produced by `npm --prefix web run build`
web/node_modules/

# QEI-Net (Beltran Urbano). Shared privately for integration work and NOT ours
# to redistribute: the weights stay unpublished until the paper is out. The
# package reads them from a path on the user's machine and never ships them.
qei_inference_package/
qei_inference_package.zip
weights/
*.pth
*.pt
*.ckpt
